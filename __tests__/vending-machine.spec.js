const { VendingMachine } = require('../src/vending-machine');

describe('Vending Machine', () => {
  
  describe('Printing Inventory', () => {
    
    it('should print the name and quantity of the products')
  })
})